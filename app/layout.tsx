import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "PUNEET MANN - CREATIVE DIRECTOR & DESIGNER",
  description: "Portfolio of Puneet Mann, Creative Director and Designer. Showcasing work across branding, statics, carousels, digital and collateral assets, and shoot direction.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${inter.className} h-full antialiased`}>
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
